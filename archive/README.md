# Arquivo / legado

- **`codigo_referencia/`** — cópia antiga de frontend e scripts; **não** é importada pelo pacote `leilao_ia_v2`. Mantida só para consulta histórica ou migração pontual.

O código de produção e testes encontra-se em `leilao_ia_v2/`.
